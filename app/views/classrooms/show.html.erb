Classroom Info
<%= render partial: 'classroom', locals: {classroom: @classroom} %>

<% Student.all.each do |student| %>
  <%= render partial: 'students/student', locals: {student: student.classroom} %>
<% end %>
